import pygame
import sys

#Initialize the module
pygame.init()

# Set up font and text
display_text = "Kaita is gay"
font = pygame.font.Font("balancescale/assets/fonts/Montserrat-VariableFont_wght.ttf", 60)
font.render(display_text, font, (0, 0, 0))